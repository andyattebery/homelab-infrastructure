network:
  host: 0.0.0.0
  port: 5000
  disable_auth: false
  api_servers:
    - OAI

model:
  model_dir: models
  model_name: Qwen3.5-9B-exl3-5.00bpw
  cache_mode: "4,4"
  cache_size: 8192

sampling:
  override_preset: safe_defaults
