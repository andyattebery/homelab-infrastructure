# Written by Ansible (podman_quadlet_gpu_encoder_sweep_node). Edits here are overwritten.
#
# This file exists ONLY because quadlet unit files are written world-readable at 0644, so anything
# secret has to live here instead of in an Environment= line. It is 0600, root-owned, and read by
# systemd before the container starts — it is not inside any mount and the container never sees the
# file, only the variable.
SWEEP_TOKEN={{ podman_quadlet_gpu_encoder_sweep_node_token }}
