---
- name: Reload systemd
  ansible.builtin.systemd_service:
    daemon_reload: true

- name: Restart shutdown_tracker_watchdog timer
  ansible.builtin.systemd_service:
    name: shutdown_tracker_watchdog.timer
    state: restarted
