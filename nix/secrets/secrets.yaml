# Placeholder -- replace with sops-encrypted secrets.yaml after setting up .sops.yaml with a real age key.
# Generate with: echo '{}' | sops --encrypt --input-type yaml --output-type yaml --config secrets/.sops.yaml /dev/stdin > secrets/secrets.yaml
{}
